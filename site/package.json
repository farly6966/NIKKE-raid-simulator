{
  "name": "nikke-calc-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run sync-runtime",
    "dev": "vite",
    "pretest": "npm run sync-runtime",
    "test": "vitest",
    "parity:engine": "tsx scripts/engine-parity.ts",
    "parity:boss-phases": "tsx scripts/boss-phase-parity.ts",
    "parity:evidence": "tsx scripts/replay-evidence.ts",
    "sync-runtime": "node scripts/sync-runtime.mjs",
    "check-runtime": "node scripts/check-runtime.mjs",
    "check-pages": "node scripts/check-pages.mjs",
    "prebuild": "npm run sync-runtime && npm run check-runtime",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "jsdom": "^29.1.1",
    "tsx": "^4.23.15",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10",
    "yaml": "^2.9.0"
  },
  "dependencies": {
    "highs": "^1.15.3"
  }
}
