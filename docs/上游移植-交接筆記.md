# 上游移植 — 交接筆記

> 給下一個接手的人（人或 AI）。目標是**不用重新對一次帳**就能繼續做。
> 最後更新：2026-09-18

---

## 0. 三十秒版本

```
fork master        ce75361   已部署，GitHub Pages 上線中
工作分支           codex/upstream-combat-core-v2   HEAD 8ae5faf   未併回 master 21 筆
upstream master    ea82f76   (Jgaram/nikke-calc)
```

upstream 全歷史 **86 筆**。`context/UPSTREAM-MAPPING.md` 目前判定了其中 **26 筆**。
**剩下的還沒逐筆判定過** —— 「進度不到一半」的感覺是準的，數字也對得上。

---

## 1. 這個 repo 跟 upstream 的關係（先看這段，不然會走冤枉路）

這個 fork **不是** `Jgaram/nikke-calc` 的 fork。

```
git merge-base ce75361 upstream/master   →  沒有共同祖先
git merge-base ce75361 Moris-kr/master   →  f56124d
```

歷史來自 `Moris-kr/nikke-calc`；`Jgaram/nikke-calc` 是同一個專案用新歷史重新發佈的
那一邊（第一個 commit 是 2026-08-23 的部署快照）。兩邊會互相搬對方的修正，所以
**同一個缺陷常常已經用不同的實作在各自那邊修好了**。

推論：

- **不要 cherry-pick。** 曾經直接 cherry-pick `5abe65e` 就撞衝突。逐筆讀 diff 做語意移植。
- **逐行比對只能拿來分類（triage），判定一律看語意。** 上游新增的 5 行程式碼在 fork
  裡可能全都有，但在**別的路徑**上，真正該修的那條是空的（`63a784e` 就是這樣）。
- **名字不一樣不代表沒有。** 例：upstream 的 `infinite_ammo`，fork 早就有，叫
  `max_ammo_infinite`。

### 判定基準（沿用 `context/UPSTREAM-MAPPING.md`）

| 判定 | 意思 |
|---|---|
| 已存在 | 同一個缺陷在 fork 已經修好了。實作不同也算 |
| 部分 | 本體有，只缺後續補強 |
| 需移植 | 這個缺陷在 fork 還活著 |
| 不適用 | 在 fork 的結構下不成立 |
| 已移植 | 做掉了，附 commit |

---

## 2. 這一輪做了什麼

### `d85ac2f` —「彈藥 N% 移除」把彈匣打成負數

`93ec10a` 殘留項之一。`ammo_charge_pct` 扣的是**最大裝彈**的比例，不是彈匣裡現有的彈。
半滿的彈匣吃到 -100% 會變負數，接下來的裝填先花掉幾發爬回 0。

fork 裡吃到 -100 的五個都是會進編成的角色：그레이브「방열」· 라플라스 : 얼티밋
히어로「일렉트릭 파워 풀 풀 차지 5」· 밀크 : 블루밍 바니「부끄러움 3」· 질
「슈퍼 캅 2」· 드레이크 : 그레이트 빌런「오버 오버 드라이브 2」。

實測（질，「슈퍼 캅 2」掛在 `burst_cast`，每循環必定發生，120 秒）：
修正前彈匣最低 **-20**，四次進負數；修正後最低 0。

`handle_ammo_charge_flat` 沒動 —— 資料裡沒有負的 flat 值，上游也只在 pct 加了下限。

### `7cf88f8` — 有限掩體到期就收掉裝填

`35d8c20` 的掩體那一半。`_finish_reload` 的 docstring 本來就記著這個缺口。

**位置比實作重要**：照抄進 `_tick_cover` 是死碼 —— `tick()` 的裝填完成檢查在它前面，
而且裝填進行中就 `return []`。所以抽成 `_expire_timed_cover()` 放在那個檢查**之前**。
第一版就是加錯位置，靠測試抓出來的。

### `997170e` — 首循環也有「下次 Full Burst 開始」預測（B 區）

`c751e2a` + `54e30af`。政策 B(`into_fb`) 與 `if_dry` 靠**前一循環週期觀測**回答
「下次 Full Burst 何時開始」，第一循環沒有觀測值就整個不作用。改成用剩餘爆裂冷卻
把階段鏈 1→2→3 往前推。

**觀測贏過鏈條** —— 鏈條看不到還沒撒下的 `burst_cooldown_reduce`（技能撒的即時效果，
不是 buff），所以只在沒有觀測時當 fallback，而且**只在 Full Burst 結束那一刻取一次**
（每 tick 重取會讓「每循環一次」的守衛失效，掩體會連開）。

實測 `레이드_작열짬`：預測誤差 **+0.020 秒**，掩體 10 → 11 次（多的就是第一循環那一次）。

### `8ae5faf` — 클릭 스케줄 `control.click`（B 區）

`8d16ea5` 的클릭 스케줄部分。톡톡이 與 홀드 是**同一次左鍵上的兩個行為**，合併成一份
清單，**先匹配者勝**。窗四個 × 模式三個，詞彙是封閉的。舊鍵 desugar 接住，
**basline 29/29 一格都沒動**。

順手拿掉 `_tick_charge` 裡重複的 `_hold_release_t < 0` 守衛 —— 按下的決定已經在
charge 開始時 latch，留著會讓排程順序失去意義（反轉順序結果相同，測試抓到的）。

---

## 3. 判定修正 —— 兩筆原本記錯的

**`ace1a61`（「무기 변경 상태」總稱判定）→ 不適用，不是缺口。**

upstream 的 parser 會吐出泛稱 `self_state:무기 변경`，所以引擎需要一個總稱分支。
fork 的 parser 直接解析成**實際模式名**：목단「다 덤벼! 2」的條件是
`self_state:정정당당 승부다!`，而牠的 `weapon_change` 項目 `name` 正是同一個字串，
字面比對就成立。fork 全檔 `self_state:무기 변경` 出現 **0 次**。

**`35d8c20` 的 `infinite_ammo` → 已存在。** fork 叫 `max_ammo_infinite`，
`test_roster_batch06` 有釘。

---

## 4. 還沒做的

### A 區 · 累積式爆裂量表 —— 剩 2 項，都卡在同一件事上

`93ec10a` 在對照表是「部分移植」。四個殘留項，這一輪清掉兩個，剩：

| 殘留項 | 狀態 |
|---|---|
| `clip_count`（CDN `reload_bullet`） | **阻塞** ↓ |
| `reload_ratio_pct`（그레이브「방열」，upstream `35d8c20`） | 依賴 `clip_count` |
| ~~「武器變更狀態」總稱判定~~ | 不適用（見 §3） |
| ~~`ammo_charge_pct` 負值下限~~ | 已移植 `d85ac2f` |

**阻塞在哪**：upstream 從 CDN `reload_bullet` 推導 `clip_count`（3300 → 3 回），
再用 `1 / clip_count` 當裝填比例，`reload_ratio_pct` 是加在上面的增益。

fork 現在用**手寫名單** `data/weapon_mechanics.json` 的 `clip_characters`
（SG 9 人 · RL 5 人）判定彈夾武器，並寫死 1/3。

要換成推導式，需要：

1. `scraper/cdn_fetch.py` 的 `무기상세` 加收 `shot.get("reload_bullet")`
   —— 欄位就在同一列 `shot`，只是現在沒收。
2. 重新抓 CDN，重建 `scraper/nikke_scraped.json`。

第 2 步是阻塞點：本機 **`httpx` 沒裝**，而且 `nikke_scraped.json` 是 AGENTS.md 指定的
**原始資料唯一正本**（200 人份），整份改寫會牽動大量衍生數字。**這需要你點頭再做**，
不該由代理自己決定。

做法跟今天的 `f52a0c6`（把「是否蓄力」從武器類型分離，改讀 CDN `조작 타입`）完全同型
—— 那次的結論是行為零改變、手寫例外退休。這次大概也是。

### B 區 · 操作與鏡頭仲裁 —— 兩塊做掉，剩仲裁那塊

| commit | 內容 | 狀態 |
|---|---|---|
| `c751e2a` · `54e30af` | 首循環 Full Burst 預測 | **已移植** `997170e` |
| `8d16ea5`（클릭 스케줄 部分） | `control.click` 統一 톡톡이／홀드 | **已移植** `8ae5faf` |
| `8d16ea5`（조작 모드 部分） | `control_mode` `solo`／`warn`／`strict` | 未移植 |
| `c961351` | 카메라 경합依等級仲裁 + 控制 pipeline | 未移植 |
| `29c7ce1` | attachment schema 統一，加入 growth/stat 條件 | 未移植 |
| `ac8fe2a` | anchor 語法、window enum、裝填控制 3 政策 | 未移植 |
| `544adeb` | 爆裂納入 control，第五按鈕與 delayed burst | 未移植 |
| `cf8c9ec` | runtime control condition 擴充 | 未移植 |
| `ec774c7` | 武器變更中控制排程不能停住 | 未移植 |

**剩下的七筆彼此咬得很緊。** `ac8fe2a` 的 anchor／gate 語法、`544adeb` 的 priority 都是
為了餵 `c961351` 的相機仲裁而存在的 —— 那是「同一時刻只有一台相機」這個約束的執行層。
fork 現在的 `camera`／`camera_mode` 是 **config 層的靜態指定**，仲裁是執行期會變的歸屬，
兩者不是同一個東西。動它會改到 `컨트롤_*` 兩條基準線，要先量再改。

`data/char_defaults.json` 目前只有 6 個角色有預設操作，`docs/聯盟突襲-交接筆記.md` §6(a)
講的「操作設定完全還沒做」指的是**聯盟那邊的隊伍層級操作 UI**，跟這裡的引擎能力是兩件事
——引擎這側現在已經能表達「本人爆裂中只按住、其餘時間點射」了。

### C 區 · 近期引擎修正 —— 清單清完了，清單本身不完整

對照表列的 18 筆判定站得住（抽驗過）。但交接文件自己警告過「不要只移植清單就宣稱
完成」，而下面這些**從沒進過對照表**，也還沒逐筆判定：

`dfadc9a`（發射機制·彈著群改 CDN 導出）· `35d8c20`（剩 `reload_ratio_pct`）·
`fb37191`（아니스·솔린 파싱 2 件）· `ffb1a6e`（再進入位重複開啟）·
`be5cfb9`（三種條件當不了閘門：防禦型分支·代碼對象·適正射程）· `0ef5382`（홍련:흑영 上修）·
`786b2ce`（支援·治療 4 件）· `c882541`（소다 2 件）· `f7f496c`（레드 후드 3 件）·
`6db7427`（技能文句全數調查 A~D）

`ace1a61` 已判定為不適用（§3）。其餘十筆**還沒看過**。

### D 區 · Boss Pattern —— 完全沒動

`boss_pattern` 在 fork 是 **0 命中**。

- `b67e366` 第 1 階段：Boss 從固定 scalar 改成依時間推進的 pattern
- `ea82f76` 第 2 階段：Boss 主動攻擊妮姬（護盾·掩體·嘲諷·隱身·戰鬥不能·復活）

這批最大，而且會動到現有的 Boss Maker、boss phases、聯盟五王選擇與網站輸入格式。
**必須保留**那些 fork-only 功能；schema 若變更要給舊設定的向後相容轉換，不能讓既有
儲存資料或匯出檔失效。

### 不在 upstream 帳上的

- **本分支 16 筆還沒併回 `master`** —— 做完但沒上線。
- `docs/聯盟突襲-交接筆記.md` §6(b)：王的防禦力仍是 `31,784`（單人突襲基準，= 敵人等級
  400）。聯盟王等級高很多，**現在算出來偏高**。這一項跟 §6(c) 的偏低互相抵銷中，
  兩邊都還沒校準。
- 同 §6(f)：使用者真實 `game_uid` 還在 `87bdf87` 的歷史裡（當前檔案樹已清）。

---

## 5. 驗證

引擎側（專案根目錄）：

```bash
python -m unittest discover -s calculator -p 'test_*.py'   # 247 條（本文件寫成時）
python -m context.doclint
python -m context.snapshot                                  # 29 條基準線
```

網站側（`site/`）：

```bash
npm ci                      # 第一次
npm run sync-runtime        # 改了 calculator/ context/ data/ 之後必跑
npm run check-runtime
npm test -- --run
npm run build
```

`site/public/runtime/` 與 `site/dist/` 都是產生物且 gitignore，不會進 commit。

**前端長測試不要和 Python snapshot 並行** —— 搶 CPU 會造成 `vi.waitFor` 假逾時
（`1707ca3` 量過：併行 576 秒 2 失敗，序列 210 秒全過）。任何失敗先單獨重跑再決定改不改碼。

快照因**正確的語意改變**而失敗時，先解釋到角色／技能／事件順序，再
`python -m context.snapshot --update`。**不可為了綠燈直接接受未知差異。**

---

## 6. 建議順序

1. **B 區剩下的仲裁那塊** —— `ac8fe2a`（anchor 語法）→ `544adeb`（priority）→
   `c961351`（相機仲裁）。前兩筆是為了餵第三筆而存在的，拆開做沒有意義。
2. **C 區補稽核** —— 十筆逐項判定，每個「已存在」都要用 targeted regression 證明。
3. **A 區收尾** —— 等 CDN 重抓那件事點頭之後。
4. **D 區** 最後，因為它會碰大量戰鬥狀態與測試。

隨手記：每做一批就更新 `context/UPSTREAM-MAPPING.md`，那份表是移植帳本的正本，
而且**跟著 git 走** —— 換機器唯一帶得過去的就是它。

---

## 7. 跟使用者合作的方式

（沿用 `docs/聯盟突襲-交接筆記.md` §8，這裡只記與移植相關的）

- **講繁體中文。** 新寫的程式註解也用繁中（既有檔案是韓文，那是上游作者的；
  fork 在既有韓文檔案裡加的新行一樣用繁中）。
- **遊戲機制以使用者為準。** 他真的在玩。repo 裡三處互相背書的值也曾經一起錯
  （戰鬥時間 90 秒 vs 實際 180 秒）。
- **他把 commit 當存檔點。** 每個階段結束跑完整測試再 commit。
- 合併回 `master` 會觸發部署（`.github/workflows/pages.yml`）。**合併前先問使用者。**
