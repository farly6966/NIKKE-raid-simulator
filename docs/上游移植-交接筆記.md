# 上游移植 — 交接筆記

> 給下一個接手的人（人或 AI）。目標是**不用重新對一次帳**就能繼續做。
> 最後更新：2026-09-18

---

## 0. 三十秒版本

```
fork master        已部署（A 區 + B 區 + C 區 + CDN 重抓）
工作分支           codex/upstream-combat-core-v2
upstream master    ea82f76   (Jgaram/nikke-calc)
```

**A 區 · B 區 · C 區完成；D 區階段 1 完成，剩階段 2（要掛 beta 旗標）。**

upstream 全歷史 **86 筆**。`context/UPSTREAM-MAPPING.md` 判定了其中 **43 筆**，
剩下 42 筆在 2026-09-19 逐筆看過一遍：合併 5 · revert 對 4 · 文件／基礎建設 23 ·
fork 已有的角色 6 · 不適用的重構 2 → **實際可做的剩 2 筆，都在 D 區**。

**C 區補稽核的收穫最大。** 八筆裡有五筆是「條件寫著卻實質恆真」或「規格寫著卻沒人讀」，
全部躲過了 baseline 的綠燈。其中 `ffb1a6e` 在上游是潛伏的，**在這個 fork 是真的會發生**
—— 兩個再進入角色同隊時 Full Burst 一次都不跑，總傷只剩 17%。

**CDN 重抓（使用者授權）解開了三項同源的阻塞**，而且發現手寫的發射機制規則
**本來就寫在資料裡** —— 手寫 36 件對推導式 34 件完全一致。

---

## 1. 這個 repo 跟 upstream 的關係（先看這段，不然會走冤枉路）

這個 fork **不是** `Jgaram/nikke-calc` 的 fork。

```
git merge-base ce75361 upstream/master   →  沒有共同祖先
git merge-base ce75361 Moris-kr/master   →  f56124d
```

歷史來自 `Moris-kr/nikke-calc`；`Jgaram/nikke-calc` 是同一個專案用新歷史重新發佈的
那一邊（第一個 commit 是 2026-08-23 的部署快照）。兩邊會互相搬對方的修正，所以
**同一個缺陷常常已經用不同的實作在各自那邊修好了**。

推論：

- **不要 cherry-pick。** 曾經直接 cherry-pick `5abe65e` 就撞衝突。逐筆讀 diff 做語意移植。
- **逐行比對只能拿來分類（triage），判定一律看語意。** 上游新增的 5 行程式碼在 fork
  裡可能全都有，但在**別的路徑**上，真正該修的那條是空的（`63a784e` 就是這樣）。
- **名字不一樣不代表沒有。** 例：upstream 的 `infinite_ammo`，fork 早就有，叫
  `max_ammo_infinite`。

### 判定基準（沿用 `context/UPSTREAM-MAPPING.md`）

| 判定 | 意思 |
|---|---|
| 已存在 | 同一個缺陷在 fork 已經修好了。實作不同也算 |
| 部分 | 本體有，只缺後續補強 |
| 需移植 | 這個缺陷在 fork 還活著 |
| 不適用 | 在 fork 的結構下不成立 |
| 已移植 | 做掉了，附 commit |

---

## 2. 這一輪做了什麼

### `d85ac2f` —「彈藥 N% 移除」把彈匣打成負數

`93ec10a` 殘留項之一。`ammo_charge_pct` 扣的是**最大裝彈**的比例，不是彈匣裡現有的彈。
半滿的彈匣吃到 -100% 會變負數，接下來的裝填先花掉幾發爬回 0。

fork 裡吃到 -100 的五個都是會進編成的角色：그레이브「방열」· 라플라스 : 얼티밋
히어로「일렉트릭 파워 풀 풀 차지 5」· 밀크 : 블루밍 바니「부끄러움 3」· 질
「슈퍼 캅 2」· 드레이크 : 그레이트 빌런「오버 오버 드라이브 2」。

實測（질，「슈퍼 캅 2」掛在 `burst_cast`，每循環必定發生，120 秒）：
修正前彈匣最低 **-20**，四次進負數；修正後最低 0。

`handle_ammo_charge_flat` 沒動 —— 資料裡沒有負的 flat 值，上游也只在 pct 加了下限。

### `7cf88f8` — 有限掩體到期就收掉裝填

`35d8c20` 的掩體那一半。`_finish_reload` 的 docstring 本來就記著這個缺口。

**位置比實作重要**：照抄進 `_tick_cover` 是死碼 —— `tick()` 的裝填完成檢查在它前面，
而且裝填進行中就 `return []`。所以抽成 `_expire_timed_cover()` 放在那個檢查**之前**。
第一版就是加錯位置，靠測試抓出來的。

### `997170e` — 首循環也有「下次 Full Burst 開始」預測（B 區）

`c751e2a` + `54e30af`。政策 B(`into_fb`) 與 `if_dry` 靠**前一循環週期觀測**回答
「下次 Full Burst 何時開始」，第一循環沒有觀測值就整個不作用。改成用剩餘爆裂冷卻
把階段鏈 1→2→3 往前推。

**觀測贏過鏈條** —— 鏈條看不到還沒撒下的 `burst_cooldown_reduce`（技能撒的即時效果，
不是 buff），所以只在沒有觀測時當 fallback，而且**只在 Full Burst 結束那一刻取一次**
（每 tick 重取會讓「每循環一次」的守衛失效，掩體會連開）。

實測 `레이드_작열짬`：預測誤差 **+0.020 秒**，掩體 10 → 11 次（多的就是第一循環那一次）。

### `8ae5faf` — 클릭 스케줄 `control.click`（B 區）

`8d16ea5` 的클릭 스케줄部分。톡톡이 與 홀드 是**同一次左鍵上的兩個行為**，合併成一份
清單，**先匹配者勝**。窗四個 × 模式三個，詞彙是封閉的。舊鍵 desugar 接住，
**basline 29/29 一格都沒動**。

順手拿掉 `_tick_charge` 裡重複的 `_hold_release_t < 0` 守衛 —— 按下的決定已經在
charge 開始時 latch，留著會讓排程順序失去意義（反轉順序結果相同，測試抓到的）。

### `44b84c6` — 조작자는 한 명（B 區）

`8d16ea5` 的조작 모드部分。實際操作是**選中一隻妮姬**後左鍵／shift；計算機原本讓多人
同時操作直接通過，`context/CONTROL.md` §미구현·보류 把它記成「同時控制 1 人約束 ——
不檢查」，`레이드_라피앨리스` 的註解也自稱「樂觀上限」。

**關鍵是把判定與執行拆開** —— 仲裁必須能在沒有副作用的情況下問政策「你現在想要嗎」。
一個函式兼兩件事的話，「只是問一下」就會把掩體打開。`_arbitrate_control()` 在
char tick **之前**跑（在裡面決定的話隊伍位次會改變答案）。

baseline `레이드_라피앨리스` +0.10%。舊行為把相機退回 3 號位 = 라피（**MG，非蓄力**），
×2.5 滿蓄力量表倍率掛在永遠用不到它的人身上 180 秒；現在跟著實際操作者走
（앨리스 99.7%、라피 0.3% ≈ 0.47 秒）。full_burst 次數 15 → 15 不變，是循環內重分配。

### `b60462a` — 카메라 경합을 등급으로（B 區）

`c961351`。純後入優先會讓全程佔著點擊的常時點射擠掉「這個時刻非做不可」的操作。
上 30（爆充點射·裝填 C）／中 20（掩體控·長按控）／下 10（常時點射·裝填 A·B）／
序列 99。**同樣是點射，目的不同等級就不同。**

baseline 一格都沒動 —— 那個編成兩個控制**都是下**。所以另外立了直接測仲裁規則本身的
測試（把 `_wants_control()` 換成固定值，只跑 `_arbitrate_control()`），免得等級沒被驗過。

### `e1b36db` — 再進入自己又開再進入就卡死（C 區）

`ffb1a6e`。`tick()` 的 `stage:` 與 `reenter:` 幾乎同樣的程式碼分成兩塊，而再進入那塊
把 `_try_use_stage()` 的第三個回傳值丟掉。再進入位選到的同伴**也是**再進入角色時
回傳 `advanced=False`，被當成「沒人用」，`_phase` 就停在 `reenter:N` —— 沒有退路。

上游只有阿妮斯：超級巨星一個再進入角色，所以那邊是潛伏。**這個 fork 有三個**：
阿妮斯：超級巨星（20 秒）· 露菲：冬日購物狂（20 秒）· 蒂亞（40 秒），全是 B1。
任兩個同隊就開。實測 60 秒：

```
 3.05  stage:1 사용    [阿妮斯：超級巨星]
 3.55  reenter:1 사용  [露菲：冬日購物狂]
23.05  reenter:1 사용  [阿妮斯：超級巨星]
23.55  reenter:1 사용  [露菲：冬日購物狂]     ← 60 秒內就只有這個
```

2·3 階段與 Full Burst **一次都沒來**。總傷 59,836,939，只換掉第二格（桃樂絲，無再進入）
的同編成是 346,097,975 —— **剩 17%**。修後 287,036,735。

兩塊合成一個分支：`used = advanced or reenter_info is not None`（`reenter_info` 回來
表示爆裂**已經放出去了**），加上 `_reenter_done_stages` —— 每階段每循環只開一次。
baseline 29/29 一格未動，因為 29 個編成裡沒有兩個再進入角色的組合。這正是回歸抓不到的
原因，所以另立 `calculator/test_burst_reenter.py` 把那個編成擺出來。

### `21a8e97`·`51cc240` — 條件閘門·規格沒人讀·去重鍵（C 區）

明細見 commit message 與 `context/UPSTREAM-MAPPING.md` §C 區。一句話版本：
`be5cfb9`（防禦型排他分支同時成立·代碼閘門·適正射程）· `786b2ce`（自身排除目標鍵·
`max_hp_additive` 規格沒人讀·值左右欄交叉·`core_hit_count:N` 完全沒匹配）·
`c882541`（索達門檻低一格·兩個 `fullburst_duration` 被壓成一個）· `f7f496c`（三件不動傷害）
· `fb37191`（阿妮斯：超級巨星蓄力固定值漏掉，本人 +18~24%）。

---

## 3. 判定修正 —— 兩筆原本記錯的

**`ace1a61`（「무기 변경 상태」總稱判定）→ 不適用，不是缺口。**

upstream 的 parser 會吐出泛稱 `self_state:무기 변경`，所以引擎需要一個總稱分支。
fork 的 parser 直接解析成**實際模式名**：목단「다 덤벼! 2」的條件是
`self_state:정정당당 승부다!`，而牠的 `weapon_change` 項目 `name` 正是同一個字串，
字面比對就成立。fork 全檔 `self_state:무기 변경` 出現 **0 次**。

**`35d8c20` 的 `infinite_ammo` → 已存在。** fork 叫 `max_ammo_infinite`，
`test_roster_batch06` 有釘。

---

## 4. 還沒做的

### A 區 · 累積式爆裂量表 —— **做完了**（2026-09-19）

使用者授權重抓 CDN 之後，`93ec10a` 的殘留兩項和 C 區 `dfadc9a` 一起落地
（`477ee18`·`2e7e775`）。三項本來就是同一個阻塞。

| 殘留項 | 狀態 |
|---|---|
| `clip_count`（CDN `재장전 탄수`） | **已移植** `477ee18` |
| `reload_ratio_pct`（그레이브「방열」，upstream `35d8c20`） | **已移植** `2e7e775` |
| ~~「武器變更狀態」總稱判定~~ | 不適用（見 §3） |
| ~~`ammo_charge_pct` 負值下限~~ | 已移植 `d85ac2f` |

**收穫比預期大。** 原本以為只是補兩個欄位，結果 `shot_detail` 裡的
`input_type`·`maintain_fire_stance` **就是手寫 `weapon_delays.json` 的那些規則**：

```
post_fire_delay    = 0                                   (DOWN_Charge)
                   = 0.22 + max(0.16, 유지/100)          (UP)
                   = 없음                                 (DOWN)
cover_during_delay = (input_type == UP and 유지 == 0)
clip_count         = max_ammo × 재장전 탄수 / 10000
```

手寫的 36 件裡 **34 件與推導式完全一致**（`cover_during_delay` 16/16 · 클립 14/14 ·
`post_fire_delay` 4/6）。不一致的兩件都有影片實測註記，留在 `_exceptions` 第①層
（那層優先於 CDN）。對照表寫在 `context/DATA_VERIFY.md` §weapon_delays.json。

`reload_ratio_pct` 是**第二軸**，這點值得記住：`reload_speed_pct` 管「一次要多久」，
`reload_ratio_pct` 管「一次填多少」→ 改變**次數**。格拉維「방열」原本誤記成
`reload_speed_pct: -50`，而兩個模型在友軍裝填速度 +50% 時剛好同值 —— 所以錯了很久沒露出來。

**副產品**：重抓帶回兩名新角色（길티 : 마이티 바니 · 신 : 스위프트 바니），已經收進
`nikke_scraped.json` 但還沒進 `parsed_skills.json`，所以網站名冊上還看不到。
要補的話走 `char-add` 流程。

### B 區 · 操作與鏡頭仲裁 —— **做完了**（2026-09-19）

| commit | 內容 | 狀態 |
|---|---|---|
| `c751e2a` · `54e30af` | 首循環 Full Burst 預測 | **已移植** `997170e` |
| `8d16ea5`（클릭 스케줄） | `control.click` 統一 톡톡이／홀드 | **已移植** `8ae5faf` |
| `8d16ea5`（조작 모드） | `control_mode` solo／warn／strict + 仲裁本體 | **已移植** `44b84c6` |
| `c961351` | 카메라 경합依等級仲裁 | **已移植** `b60462a` |
| `ec774c7` | 武器變更中控制排程不能停住 | **已移植** `2e7e775` |
| `544adeb` | 爆裂納入 control·delayed burst | **已移植（機制）** `2e7e775` |
| `cf8c9ec` | runtime control condition 擴充 | **已移植** `0452e4b` + 最新 |
| `29c7ce1` | attachment schema 統一 | **不適用** |
| `ac8fe2a` | anchor 語法（`fb_end@+2+4`） | **已移植** |

**`ac8fe2a` 是這一區最大的一筆**（上游 430 行）。它把「언제」收成一行
`anchor + offset − minus`，裝填控制三政策折進去。兩件事值得記住：

- **不要把狀態窗一起拿掉。** `burst_charge` 的判定是 `state["burst_gauge_charging"]`，
  而那正是量表加算判斷「現在在充電嗎」用的同一個值。換算成時刻，那個「不可能錯開」的
  保證就沒了。
- **回歸 0 的根據是「錨點自帶閘門」。** 政策 A·C 原本自己問 `full_burst`、B 自己問有沒有
  觀測值 —— 這些條件搬進錨點定義之後，判定式才會**逐字相同**。`full_burst_end_t`
  在 Full Burst 結束後還留在 state，沒閘門就會讓「結束前 0.3 秒」永遠為真。

fork 比上游多做兩件：`reload.len` 不再被默默丟掉（上游那邊還會），以及禁止把
`combat_start` 配給放開模式（每循環一次的守衛會變成每場一次）。新增 doclint 檢查 I ——
`_control_rules` 要條件符合的隊伍才會走到組裝，**沒人跑過的規則**的錯字只有那裡抓得到。

**`cf8c9ec` 的兩件也收掉了，B 區全清。** 兩件都踩在同一條線上——**觀察不能改變戰鬥**：

- `gate: {burst_stage, burst_user}` 只看**已經確定**的本循環各階段爆裂使用者，
  不預測接下來誰會用，所以這個閘門本身改不了循環。紀錄在**下一個循環進入 1 階段時**
  才清空——這樣 Full Burst 結束**前**的裝填控制和結束**後**的充電窗看到同一個 B3 使用者。
- `own_buff_end` 錨點（+ 裝填控制政策 D）**不查效果的對象**。查了就會提前那個增益的
  延遲 resolve，變成「控制看了一眼，結果就不一樣」。

fork 多做一件：`simulate()` 開跑前檢查名字指到的東西存在（`_check_control_refs()`）——
組裝時還不知道隊伍和技能表，那裡是第一個能檢查的位置。打錯字活下來的話，閘門和錨點
永遠不會開，就成了「開了控制但什麼都沒發生」。

**「什麼時候」的詞彙現在只剩一個空位**：問**增益對象**的條件
（`context/CONTROL.md` §미구현·보류）。要開那扇門得先解延遲 resolve 的決定時點問題。

`data/char_defaults.json` 目前只有 6 個角色有預設操作，`docs/聯盟突襲-交接筆記.md` §6(a)
講的「操作設定完全還沒做」指的是**聯盟那邊的隊伍層級操作 UI**，跟這裡的引擎能力是兩件事。

### C 區 · 補稽核 —— 逐筆看完了，剩兩筆有明確理由

十筆全部逐筆判定完，明細在 `context/UPSTREAM-MAPPING.md` §C 區。結果：

- **已移植七筆**：`ffb1a6e`（`e1b36db`）· `fb37191`（`21a8e97`）· `be5cfb9`·`786b2ce`·
  `c882541`·`f7f496c`（都在 `51cc240`）· `35d8c20` 的掩體那半（先前 `7cf88f8`）
- **不適用一筆**：`0ef5382` —— fork 的 `test_priority_skill_data.py` 逐級對照原文，10 級全符
- **不移植一筆**：`6db7427` —— `calculator/` 零改動，純資料 316 行。這是上游對**自己**名冊
  的原文普查結果，照搬等於重做一次 fork 的普查。**所以在 fork 這邊自己做了**，見下面
  §fork 側原文全數普查
- **阻塞一筆**：`dfadc9a` —— 要 CDN `roledata.shot_detail` 的 20 個欄位。
  `scraper/nikke_scraped.json` 裡 `shot_detail`·`maintain_fire_stance`·`input_type`·
  `rate_of_fire` 出現次數**全部是 0**。與 A 區 `93ec10a` 的 `clip_count`／
  `reload_ratio_pct` **同一個阻塞**：都要先重抓 CDN

**這一區留下的未解一件**：`calculator/test_reload_delay_speed.py` 原本釘「阿妮斯：閃耀夏日
排在梅登：冰玫瑰前面」。那不是這個 repo 使用者的實測，是從 `Moris-kr/nikke-calc` 的
`e028cbc`（2026-08-25）繼承的，那筆 commit 自己寫著修完是「阿妮斯 40.2% · 梅登 35.7%」。

梅登那半是 `max_hp_additive` 修好的（33.3% → 35.6%，提報 35.2%）。

**阿妮斯那半用 `git bisect` 查過了 —— 不是回歸。**（`e028cbc` 用今天的養成跑出來正好
40.2%，跟那筆 commit 寫的一模一樣，所以也排除了「養成不同」。）她的**絕對傷害從頭到尾
都是 `2,328,198,978`，一格沒動過**。比重下降是分母變大：`2584f4d`（moris-kr, 2026-08-30）
修好牡丹「武器變更 5 打追加傷害」靜默失效，牡丹 `919,803,289 → 1,432,463,148`（+55.7%）。
那也是使用者提報驅動的正當修復，跟 C 區抓到的是同一種毛病。

**真正的疑點：兩份提報彼此不相容。** 42.1% + 35.2% 表示其餘三人共 22.7%，但牡丹修好後
一個人就 21.4%，那愛德 + 普麗瓦蒂要合起來 1.3%（模擬 7.0%）。兩份提報**都來自 upstream
那邊**，很可能是不同帳號·不同養成。

**所以這件事不是「去修阿妮斯」，而是「這個目標數字對不對」。** 要判定只有一條路：
請這個 repo 的使用者用這個編成（牡丹·愛德：特務兔女郎·阿妮斯：閃耀夏日·梅登：冰玫瑰·
普麗瓦蒂，水冷王有核心）**一次量五個人的完整比重**。

### fork 側原文全數普查（2026-09-19）—— 做完了

`6db7427` 該做的事在 fork 這邊自己做了一遍。把抓錯時用的臨時掃描做成 **6 個不變式**，
跑過 200 人全部原文，常駐在 `calculator/test_raw_text_audit.py`（0.03 秒，留著不心疼）。
逐項覆蓋與結果在 `context/UPSTREAM-MAPPING.md` §fork 側原文全數普查。

- **抓到 2 個真錯**：索林：霜雪車票的 `첫차 할인` buff 整個沒被解析出來（連帶
  `remove_named_buff` 在解除不存在的東西、車票豁免整個缺席）；阿妮斯：超級巨星
  `슈팅 스타4` 的平白 `[방어력 N% ▲]` 被記成 caster_based
- **1 筆待使用者判定**：瑪爾恰娜：海洋學習 `휘슬` 的 `max_stack` 是 9 還是 5。名冊慣例
  指向 5，但現行值有刻意的註解，差距是她本人 −31.9%。沒有自己動
- 值的部分（1,362 個區塊、446 個技能區塊的欄位順序）**零真錯** —— 等級表是乾淨的

### D 區 · Boss Pattern —— 階段 1 做完了，剩階段 2

| commit | 內容 | 狀態 |
|---|---|---|
| `b67e366` | 階段 1 —— Boss 依時間推進的腳本 | **已移植** |
| `ea82f76` | 階段 2 —— 王反打妮姬 | 未移植（要掛 beta 旗標） |

**先更正一個先前的誤判**：交接筆記原本寫「`boss_pattern` 在 fork 是 0 命中」，那讓人以為
階段 1 是個全新子系統。其實**這個 fork 本來就有時間軸的王階段系統** ——
`enemy["boss_phases"]`，六種（core·optimal_range·pierce_gate·immune·element_gate·parts），
每種帶 `from`/`to`，`calculator/test_union_boss_phases.py` 在釘它。階段 1 是**把已有的東西
一般化**，不是從零長出來。

**階段 1 的合格門檻很硬，而且它自己保證得到**：`enemy["patterns"]` 為空就不建排程器，
所以 baseline 29/29 結構上不可能動。真正要證明的是**疊加接線沒和基本路徑走岔** ——
`legacy_to_patterns()`（上游註明「檢算專用，引擎不用」）把舊 scalar 四項寫成「開場開到底的
pattern」，整場戰鬥總傷與各角色傷害必須完全相同。

⚠️ **那個等價測試需要編成裡有「爆裂會直接打傷害」的角色**。`BurstController.enemy_def`
如果在 `__init__` 就抓住值，只有爆裂傷害會用到舊防禦力；全是增益型爆裂的編成看不出差別。
我故意把 property 改回快取驗證過：拉毗：小紅帽在隊裡才會紅（1,603,915,561 vs 1,610,739,121）。

**兩軸目前互不知道。** `boss_phases` 和 `patterns` 同時給的話，窗那邊會後寫覆蓋。沒有硬轉
過去的理由有兩個：`pierce_gate` 上游沒有對應，而 fork 的 `immune`（擋全部傷害）和上游
`vanish`（只擋平A）**語意不同** —— 硬轉會悄悄改掉既有設定的意思，包括使用者存過的
Boss Maker 盤面。統一要另外立等價證明再做。

**階段 2 是另一回事。** 它改變「計算機在算什麼」——目前整個引擎假設妮姬不會受傷。
而且時機與傷害數字只有使用者能校準。做法照講好的：掛 `beta` 旗標、預設關、關著的時候
結果與現在完全相同（用 baseline 釘死）。

### 不在 upstream 帳上的

- 部署是 fast-forward `master`（會觸發 `.github/workflows/pages.yml`）。**綠燈不等於上線** —— 實際抓線上頁面確認資產指紋換了才算。
- `docs/聯盟突襲-交接筆記.md` §6(b)：王的防禦力仍是 `31,784`（單人突襲基準，= 敵人等級
  400）。聯盟王等級高很多，**現在算出來偏高**。這一項跟 §6(c) 的偏低互相抵銷中，
  兩邊都還沒校準。
- 同 §6(f)：使用者真實 `game_uid` 還在 `87bdf87` 的歷史裡（當前檔案樹已清）。

---

## 5. 驗證

引擎側（專案根目錄）：

```bash
python -m unittest discover -s calculator -p 'test_*.py'   # 334 條（2026-09-19，含 1 skip · 1 expectedFailure）
python -m context.doclint
python -m context.snapshot                                  # 29 條基準線
```

網站側（`site/`）：

```bash
npm ci                      # 第一次
npm run sync-runtime        # 改了 calculator/ context/ data/ 之後必跑
npm run check-runtime
npm test -- --run
npm run build
```

`site/public/runtime/` 與 `site/dist/` 都是產生物且 gitignore，不會進 commit。

**前端長測試不要和 Python snapshot 並行** —— 搶 CPU 會造成 `vi.waitFor` 假逾時
（`1707ca3` 量過：併行 576 秒 2 失敗，序列 210 秒全過）。任何失敗先單獨重跑再決定改不改碼。

快照因**正確的語意改變**而失敗時，先解釋到角色／技能／事件順序，再
`python -m context.snapshot --update`。**不可為了綠燈直接接受未知差異。**

### 上線驗證：不要用 runtime version 比對本機與線上

`site/scripts/sync-runtime.mjs` 的 `manifest.version` 是檔案內容的 sha256 前 16 碼，
而它 **把換行也算進去**。這台機器是 Windows（CRLF），CI 是 Linux（LF），所以
**同樣的內容在兩邊一定會算出不同的 hash**。

2026-09-18 實測過一次：線上 `3928534973d4366e` vs 本機 `762216b509b25874`，25 個檔案
有 22 個不同——逐位元組比對後，每一個差異的位元組數**正好等於該檔的 CRLF 數量**，
語意完全相同。差點被誤判成部署失敗。

所以線上驗證要看**內容**不是 hash：

```bash
# 資產指紋換了沒（這個可靠 —— runtime version 有嵌進 JS bundle，內容一變 hash 就變）
curl -s https://farly6966.github.io/NIKKE-raid-simulator/ | grep -oE 'assets/index-[A-Za-z0-9_-]+\.(js|css)'
# 引擎改動真的上去了沒 —— 抓線上 runtime 的原始碼找標記
curl -s https://farly6966.github.io/NIKKE-raid-simulator/runtime/calculator/timeline.py | grep -c '_reenter_done_stages'
```

**待辦（小）**：讓 `sync-runtime` 在算 hash 前把換行正規化成 LF，這個陷阱就消失了。

---

## 6. 建議順序

C 區已清完（§4）。接下來：

1. ~~`git bisect` 找阿妮斯：閃耀夏日的 5pp 回歸~~ **做完了：不是回歸**（§4 C 區末段）。
   結論改成一句給使用者的請求：用那個編成**一次量五個人的完整比重**。
2. **判定「瑪爾恰娜：海洋學習 `휘슬` 上限是 5 還是 9」** —— 唯一一條卡在你身上的機制問題，
   而且影響不小（她本人 ±32%）。細節在 `context/UPSTREAM-MAPPING.md` §fork 側原文全數普查。
3. ~~**B 區剩五筆**~~ **做完了**。八筆移植 + `29c7ce1` 判定不適用。
4. ~~**A 區 + `dfadc9a` 一起收**~~ **做完了**（使用者 2026-09-19 授權重抓 CDN）。
5. **兩名新角色** —— 重抓帶回 길티 : 마이티 바니 · 신 : 스위프트 바니，
   已進 `nikke_scraped.json`、還沒進 `parsed_skills.json`。走 `char-add`。
6. ~~**王的防禦力校準**~~ —— 使用者 2026-09-19 指示：**參考就好，不校準**。
7. ~~**D 區**~~ 階段 1 做完了。剩下兩件，互相獨立：
   - **階段 2**（`ea82f76`，王反打妮姬）—— beta 旗標，預設關。
   - **`boss_phases` ↔ `patterns` 統一** —— 要先立等價證明，而且 `pierce_gate`·`immune`
     的語意差異要先有結論（見 §4 D 區）。

**注意順序的理由**：2~4 會改變引擎算出來的數字。所以「請使用者用真實隊伍量一次」
要排在它們**之後**，否則量到的基準會在下一批移植後失效，等於白量。

隨手記：每做一批就更新 `context/UPSTREAM-MAPPING.md`，那份表是移植帳本的正本，
而且**跟著 git 走** —— 換機器唯一帶得過去的就是它。

---

## 7. 跟使用者合作的方式

（沿用 `docs/聯盟突襲-交接筆記.md` §8，這裡只記與移植相關的）

- **講繁體中文。** 新寫的程式註解也用繁中（既有檔案是韓文，那是上游作者的；
  fork 在既有韓文檔案裡加的新行一樣用繁中）。
- **遊戲機制以使用者為準。** 他真的在玩。repo 裡三處互相背書的值也曾經一起錯
  （戰鬥時間 90 秒 vs 實際 180 秒）。
- **他把 commit 當存檔點。** 每個階段結束跑完整測試再 commit。
- 合併回 `master` 會觸發部署（`.github/workflows/pages.yml`）。**合併前先問使用者。**
